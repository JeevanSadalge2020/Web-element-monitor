{
  "name": "web-element-monitor",
  "version": "1.0.0",
  "description": "Web Element Monitor - Track changes to UI elements across websites",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run dev\"",
    "server": "cd backend && npm run start",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}